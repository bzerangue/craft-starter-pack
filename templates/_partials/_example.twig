<!-- Some Utility Examples (commented out to not break stuff) -->
<!-- Lazy Image Macro Example: -->
{# {% set img = slide.image[0] %}
{% set imgU = slide.image[0].getUrl('slider') %}
{% set imgH = slide.image[0].getHeight('slider') %}
{% set imgW = slide.image[0].getWidth('slider') %}
{% set imgC = 'additional-class' %}
{{utils.lazyImg(img, imgU, imgW, imgH)}} #}

<!-- Output: -->
{# <img src="tiny-thing.jpg" data-src="actual-thing.jpg" class="lazyload blur-up additional-class"> #}