{# Main Menu #}
{# {% set menuName = craft.navee.getNav('navee-handle-here') %}
{% nav node in menuName %}
    <li{% if node.class %} class="{{ node.class }}"{% endif %}>
    	{% set target = node.target %}
		<a href="{{ node.link }}" {% if target|length %}target="{{target}}"{% endif %}>{{ node.title }}</a>
    </li>
{% endnav %} #}